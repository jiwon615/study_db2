spring:
  profiles:
    active: local
  datasource:
    url: jdbc:h2:tcp://localhost/~/test
    username: sa
    password:

#jdbcTemplate sql log
logging:
  level:
    org:
      springframework:
        jdbc: DEBUG