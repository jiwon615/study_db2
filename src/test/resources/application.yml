spring:
  profiles:
    active: test
  datasource:
#    url: jdbc:h2:tcp://localhost/~/testcase
#    username: sa
#    password:

#jdbcTemplate sql log
logging:
  level:
    org:
      springframework:
        jdbc: DEBUG