spring:
  profiles:
    active: local
  datasource:
    url: jdbc:h2:tcp://localhost/~/test
    username: sa
    password:

#jdbcTemplate sql log
logging:
  level:
    org:
      springframework:
        jdbc: DEBUG
    hello.itemservice.repository.mybatis: trace # MyBatis에서 실행되는 쿼리 로그 확인 가능
# MyBatis
mybatis:
  type-aliases-package: hello.itemservice.domain
  configuration:
    map-underscore-to-camel-case: true # false가 디폴트